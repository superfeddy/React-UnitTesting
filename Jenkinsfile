pipeline {
  agent any

  tools {nodejs "node10"}  

  stage('Install dependencies') {

    steps {
        sh 'npm -v'
      }
    }     

    stage('Test') {
      steps {
         sh 'npm -v'
      }
    }             
  }
}